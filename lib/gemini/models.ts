import { ThinkingLevel } from '@google/genai'

// ชื่อรุ่นโมเดลทั้งหมดอยู่ที่นี่ที่เดียว
//
// **เลิกใช้ alias `gemini-flash-latest` แล้ว** เดิมเลือกใช้เพราะคิดว่า alias จะพา
// เราข้ามการเลิกใช้รุ่นเก่าไปเอง แต่ราคาที่จ่ายคือมันขยับได้โดยเราไม่ได้ deploy อะไร
//
// เกิดขึ้นจริง: วันที่ 2026-09-06 alias ชี้ไปที่ `gemini-3.8-flash` ซึ่งเป็น thinking
// model วัดแล้วพบว่า **output 87-90% เป็น thinking token ที่เราจ่ายเงินแล้วทิ้ง**
// การสกัดเงื่อนไขค้นหาซึ่งวิ่งทุกครั้งที่มีคนกดค้นหา ใช้เวลา 2.5 วินาทีและแพงกว่า
// ที่ควรเป็น 10 เท่า โดยไม่มีใครสั่งให้เปลี่ยนอะไรเลย
//
// **เพิ่มรุ่นใหม่หรือเปลี่ยนรุ่นต้องแก้ที่ไฟล์นี้เท่านั้น** มีเทสต์ดักไม่ให้ไฟล์อื่น
// ใน lib/ และ app/ เขียนชื่อรุ่นตรงๆ

// งานที่ต้องอ่านเอกสารหรือเขียนข้อความให้คนอ่าน — อ่าน PDF แบบ multimodal
// และเขียนบทวิเคราะห์ภาษาไทย
//
// **ราคาแนะนำตัวหมด 31 ธ.ค. 2026 แล้วขึ้นเท่าตัว** ($0.75/$3.75 -> $1.50/$7.50)
//
// เหลือผู้ใช้สองราย หลังวัดเมื่อ 2026-09-08 (ดูตารางใน CLAUDE.md):
//   parsePdf  — คู่กับ THINKING_LOW ข้างล่าง
//   generate  — สร้าง seed data ตอนพัฒนา ไม่กระทบผู้ใช้
export const MODEL_TEXT = 'gemini-3.8-flash'

// งานสกัดโครงสร้างสั้นๆ ที่วิ่งบ่อยและไม่ต้องใช้การให้เหตุผล
//
// วัดเทียบแล้ว (2026-09-07, scripts/compare-gemini-models.ts): ผลลัพธ์ตรงกับ
// 3.8-flash ทุกฟิลด์ แต่เร็วกว่า 2.9 เท่า (854ms vs 2501ms) และถูกกว่า 10 เท่า
// ($0.19 vs $1.98 ต่อ 1,000 ครั้ง) เพราะไม่มี thinking token เลย
export const MODEL_FAST = 'gemini-3.5-flash-lite'

// 768 มิติเป็นข้อบังคับ — ต้องอยู่สเปซเดียวกับตาราง jobs และคอลัมน์
// candidates.embedding ที่เป็น vector(768)
export const MODEL_EMBED = 'gemini-embedding-001'

// **การตั้งค่าที่กระทบราคาอยู่ที่ไฟล์นี้ด้วย ไม่ใช่แค่ชื่อรุ่น**
// thinkingLevel ไม่ใช่ชื่อรุ่นจึงไม่เข้าข่ายเทสต์ที่ดักชื่อรุ่นใน models.test.ts
// แต่มันเปลี่ยนราคาได้มากกว่าการเปลี่ยนรุ่นเสียอีก จึงเก็บไว้ที่เดียวกัน
//
// วัดกับ CV จริงเมื่อ 2026-09-08: parsePdf บน 3.8-flash เต็มรูปแบบใช้ output
// 4,536 token โดยเป็นการคิด 3,181 (70%) ใช้เวลา 13.9 วินาที และ $19.09 ต่อ 1,000 ครั้ง
// **ตั้ง LOW แล้วสกัดได้เท่ากันเป๊ะ** (education 5 · experience 10 · industry เดียวกัน)
// แต่เหลือ 6.6 วินาที และ $7.12 — การคิด 3,181 token นั้นไม่ได้ซื้ออะไรเพิ่มเลย
// ใช้ enum ของ SDK ไม่ใช่สตริง 'LOW' ดิบ — SDK ประกาศ thinkingLevel เป็น enum
// การเขียนสตริงตรงๆ จะไม่ผ่าน tsc และถ้าวันไหน SDK เปลี่ยนค่า เราจะรู้ตอนคอมไพล์
export const THINKING_LOW = { thinkingLevel: ThinkingLevel.LOW } as const

# แผนแก้หลัง UAT — 17 ก.ย. 2026

ผลการทดสอบ: **91 เคส · ผ่าน 84 · ไม่ผ่าน 7** (`docs/uat/results.md`)

**7 ข้อที่ไม่ผ่านไม่ได้เป็นเรื่องเดียวกัน** — มีแค่ 2 ข้อที่เป็นบั๊กจริง อีก 5 ข้อคือ
เคสที่เขียนไว้ผิดหรือทดสอบผ่านหน้าจอไม่ได้ตั้งแต่ต้น การแยกสองอย่างนี้ออกจากกันคือ
สาระของเอกสารฉบับนี้ **ถ้าเหมารวมว่า "7 บั๊ก" จะเสียเวลาไปแก้โค้ดที่ไม่ได้ผิด**

---

## กอง A — แก้โค้ด

### A1 · SR-04 — **เกณฑ์ตั้งไม่ได้ แต่เจอบั๊กที่ใหญ่กว่าระหว่างวัด**

**วัดแล้ว 18 ก.ย. — ตั้งเส้นแบ่งไม่ได้จริง**

| คำค้น | ชิปที่ AI แยก | คะแนนสูงสุด |
|---|---|---|
| Supply Chain Manager | ตำแหน่งเท่านั้น | 67 |
| Finance Analyst ประสบการณ์ 3 ปี | ตำแหน่ง + ประสบการณ์ | 66 |
| พระสงฆ์ → Monk | ตำแหน่งเท่านั้น | **64** |
| นักบินอวกาศ → Astronaut | ตำแหน่งเท่านั้น | **59** |
| กรรมการมวย → Boxing Referee | ตำแหน่งเท่านั้น | **56** |

คำที่ควรเจอสูงสุด 67 · คำที่ไม่ควรเจอใครสูงสุด 64 — **ห่างกัน 3 แต้ม ทับกันสนิท**
ไม่มีเลขไหนแยกสองกลุ่มนี้ได้ **การใส่เลขมั่วลงไปจะเตือนผิดทั้งสองทาง**
→ ตกไป เขียนลงเล่มเป็นคุณสมบัติของการค้นด้วยเวกเตอร์ (ดูกอง C)

#### แต่ระหว่างวัดพบว่าคำค้นที่เป็นหัวใจของระบบคืนศูนย์ — **แก้แล้ว**

`Data Scientist` และ `UX Designer` ได้ "ไม่พบผู้สมัคร" ทั้งที่เป็นตัวอย่างที่ใช้
โฆษณาระบบนี้ทั้งในคู่มือและในเล่ม **ผลลัพธ์กลับด้านกับที่ควรเป็น** — คำที่ระบบ
ตอบได้ดีที่สุดว่างเปล่า ส่วนคำที่ไม่ควรมีใครตรงกลับคืน 20 คน

สาเหตุ: AI แยกชื่อตำแหน่งออกมาเป็นชิปสกิล (`Data Scientist` → สกิล `Data Science`)
แล้วชิปสกิลเป็น**ตัวกรองแข็ง** — `match_candidates_filtered` เทียบ**ตรงตัวเป๊ะ
และต้องครบทุกตัว** (`lower(s.name) = any(...)` แล้ว `count = array_length`)

**หัวไฟล์ migration 007 เขียนบทเรียนนี้ไว้เองแล้วครึ่งหนึ่ง** — field/degree เปลี่ยน
จาก exact เป็น ILIKE เพราะ *"Exact equality was too strict"* แต่ไม่เคยเอามาใช้กับ
skills ซึ่งเป็นช่องที่ AI เดาค่าได้อิสระที่สุด

**แก้ผิดรอบแรก และนั่นคือส่วนที่ควรจำ** — รอบแรกเช็คว่า "ชื่อนี้มีแถวในตาราง
`skills` ไหม" ซึ่งยังพังอยู่ ตรวจกับฐานจริงพบว่า `Data Science` **มีแถวอยู่ แต่มี
ผู้สมัคร 0 คน** จึงผ่านด่านไปเป็นตัวกรองแล้วกรองทุกคนทิ้ง โดยไม่ขึ้นข้อความอธิบาย
ด้วยเพราะโค้ดถือว่ามันเป็นสกิลที่ใช้ได้ · ฐานนี้มี **487 ชื่อ แต่ผูกกับผู้สมัครแค่ 197 ครั้ง**
— สกิลส่วนใหญ่เป็นกำพร้า **สกิลกำพร้ากรองคนทิ้งได้เท่ากับชื่อที่ไม่มีอยู่จริง**

เงื่อนไขที่ถูกคือ **"มีผู้สมัครถือสกิลนี้ไหม"** อ่านจาก `candidate_skills`
(`lib/search/knownSkills.ts` + `lib/search/query.ts`) ชิปที่ไม่มีใครถือจะไม่ถูกใช้
กรอง **แต่ยังแสดงบนจอพร้อมข้อความว่าไม่ได้ใช้** ไม่ซ่อนทิ้งเงียบๆ

**รอบแรกผ่านการทดสอบด้วยความบังเอิญ** — `UX` ไม่มีแถวอยู่จริง จึงตรงกับเงื่อนไข
ที่เขียนผิด ถ้าทดสอบด้วย `UX Designer` อย่างเดียวจะสรุปว่าแก้เสร็จแล้ว

### A1 เดิม (เก็บไว้เป็นบันทึกเหตุผล) · แถบเตือนเมื่อไม่มีใครใกล้เคียงพอ

**อาการ** ค้น "กรรมการมวย" ได้ผู้สมัคร 20 คน คนแรกคะแนน 56 เป็นสาย
Investments / Events / Real Estate

**สาเหตุ ไม่ใช่บั๊ก** การค้นด้วยเวกเตอร์คืนเพื่อนบ้านที่ใกล้ที่สุดเสมอ
ระบบไม่มีแนวคิดเรื่อง "ใกล้ไม่พอ" จึงไม่มีอะไรจะบอกว่าไม่พบ

**ทางที่เลือก: เตือน ไม่ซ่อน** — แสดงผลครบเหมือนเดิม แต่ขึ้นแถบบอกเมื่อคะแนน
สูงสุดต่ำกว่าเกณฑ์ว่าไม่มีใครใกล้เคียงมากนัก

**เหตุผลที่ไม่ตัดผลลัพธ์ทิ้ง** เลขเส้นแบ่งต้องมาจากการวัด ไม่ใช่การเดา และการ
ซ่อนคนที่ควรเจอโดยไม่มีอะไรบอกคือความผิดพลาดชนิดเดียวกับที่โปรเจกต์นี้เจอมาแล้ว
หลายรอบ — ชิปตัวกรองที่ทำงานอยู่แต่มองไม่เห็น · `width: NaN%` · "ไม่พบผลลัพธ์"
ที่ขึ้นตอนระบบค้นหาพัง **ผลลัพธ์ที่หายไปเงียบๆ แย่กว่าผลลัพธ์ที่ไม่ดีแต่มองเห็น**

- เกณฑ์เป็นค่าคงที่ที่เดียว มีเทสต์ hardcode คู่ไว้
- ข้อความต้องไม่ใช้คำว่า "ไม่พบ" เพราะผลลัพธ์ยังอยู่ครบ

### A2 · DM-03 — **ไม่ใช่บั๊ก ตัดออกจากกอง A**

ทดสอบซ้ำ 18 ก.ย. พร้อมภาพหน้าจอ: แถว `synthetic` (`Anon Bunyarat`) ขึ้นเฉพาะ
ข้อความ "ข้อมูลการศึกษา ประสบการณ์ ทักษะ ... กู้คืนไม่ได้" **ไม่มีกรอบเตือนเรื่อง
นำเข้าซ้ำ ไม่มีช่องติ๊ก "ห้ามนำเข้าอีก"** ซึ่งตรงกับที่ DM-03 กำหนดไว้ทุกประการ

`isReimportable` (`lib/candidates/remove.ts`) คืน false สำหรับ synthetic อยู่แล้ว
และมีเทสต์ครอบทั้งสามสาขา — **โค้ดถูกมาตลอด บันทึกผลเดิมอ่านคลาดเคลื่อน**
สิ่งที่ผู้ทดสอบเห็นคือคำเตือนเรื่องการลบถาวร ซึ่งขึ้นทุกแถวโดยตั้งใจ

**สองอย่างที่ต่างกันและอยู่คนละคอมโพเนนต์:**

| สิ่งที่เห็น | คอมโพเนนต์ | ขึ้นเมื่อไร |
|---|---|---|
| "…กู้คืนไม่ได้" | `DeleteCandidatesDialog` | ทุกแถว — การลบเป็นเรื่องถาวรเสมอ |
| กรอบส้ม + ช่องติ๊ก | `DeleteCandidatesDialog` | เฉพาะ `scraper` **ที่มี `linkedin_url`** |
| "ลบ X และห้ามนำเข้าอีก" + ช่องเหตุผล | `SuppressButton` | เมื่อผู้ใช้กดปุ่มระงับเองบนหน้าผู้สมัคร |

**ข้อสังเกตที่ได้จากการทดสอบซ้ำ** — `Anish Goyal` เป็น `scraper` แต่ก็ไม่ขึ้นกรอบส้ม
เพราะ `linkedin_url` ว่าง (สอดคล้องกับป้าย "ไม่มีสรุปโปรไฟล์" และ 0 ปีในตาราง)
ยืนยันว่าเงื่อนไขสองชั้นทำงานจริง ไม่ใช่ดูแค่ `source`

### A3 · AU-04 — ข้อความ error จาก Supabase ออกจอดิบ — **ทำแล้ว**

ตัวแปลงอยู่ที่ `lib/auth/authMessages.ts` ใช้ทั้ง `/login` และ `/signup`
**เทียบด้วยคำสำคัญ ไม่ใช่ข้อความเต็ม** เพราะ Supabase ไม่ได้รับประกันถ้อยคำ
และเปลี่ยนได้ทุกเวอร์ชัน — เทียบเต็มประโยคจะพังเงียบๆ ตอนอัปเกรด
ส่วนที่แปลไม่ได้ตกไปที่ข้อความกลาง ไม่ปล่อยของดิบออกไป

`login/page.tsx:20` และ `signup/page.tsx:23` ทำ `setMsg(error.message)` ตรงๆ
ผู้ใช้จึงเห็น "Invalid login credentials"

- ทำตัวแปลงข้อความที่เดียว ใช้ทั้งสองหน้า
- **ข้อความล็อกอินต้องไม่บอกว่าอีเมลมีอยู่จริง** → "อีเมลหรือรหัสผ่านไม่ถูกต้อง
  กรุณาลองใหม่" · **ห้ามใช้ "รหัสผ่านไม่ถูกต้อง"** เพราะจะทำให้ AU-04 ที่ผ่านแล้ว
  กลายเป็นไม่ผ่าน — หน้าสมัครยอมเผยเรื่องนี้โดยตั้งใจ แต่หน้าล็อกอินไม่ยอม
- ข้อความที่แปลงไม่ได้ให้ใช้ข้อความกลาง และ `console.error` ตัวเต็มไว้ฝั่งเซิร์ฟเวอร์
  (หลักเดียวกับข้อห้ามส่งข้อความ Postgres ดิบออกไป)

### A4 · AU-03 — หน้าลิงก์หมดอายุชี้ทางผิด — **ทำแล้ว**

ข้อความบอกให้ "ลองเข้าสู่ระบบ" ซึ่งใช้ไม่ได้กับบัญชีที่ยังไม่ยืนยัน แล้วบอกให้
สมัครใหม่ ซึ่งไปเจอ "อีเมลนี้ถูกใช้สมัครแล้ว" **ทั้งที่การสมัครซ้ำคือสิ่งที่ทำให้
Supabase ส่งอีเมลยืนยันฉบับใหม่จริงๆ** (`lib/auth/signupFlow.ts` บรรทัด 6-8)

**พิสูจน์แล้ว 18 ก.ย. — อีเมลฉบับที่สองมาจริง** (20:50 และ 21:03 กล่องจดหมายขึ้น
"Supabase Auth 2") การสมัครด้วยอีเมลเดิมที่ยังไม่ยืนยันทำให้ Supabase ส่งลิงก์ใหม่
ผู้ใช้จึงไม่ตัน **แต่ไม่มีข้อความไหนในระบบบอกเรื่องนี้เลย** ต้องบังเอิญไปเปิด
กล่องจดหมายเอง — เป็นการแก้ข้อความ ไม่ใช่การเพิ่มฟีเจอร์

**ระหว่างทดลองเจอตัวรบกวนที่เตือนไว้จริง** — รอบแรกชนโควตา SMTP แล้วหน้าสมัคร
ขึ้น `email rate limit exceeded` เป็นภาษาอังกฤษดิบ **ซึ่งทำให้ผู้ใช้สรุปว่าอีเมลนี้
ใช้สมัครไม่ได้ ทั้งที่ความจริงคือรอสักครู่แล้วได้** สองอย่างนี้ต้องทำคนละอย่างกัน
→ A3 ต้องมีกรณีนี้ด้วย: "ระบบส่งอีเมลถึงขีดจำกัดชั่วคราว กรุณารอสักครู่แล้วลองใหม่"

**ข้อความใหม่ของหน้าลิงก์หมดอายุ** ต้องครอบทั้งสองสภาวะ เพราะหน้านั้นไม่รู้ว่า
บัญชียืนยันแล้วหรือยัง:

> ลิงก์นี้หมดอายุหรือถูกใช้ไปแล้ว
> · ถ้ายืนยันอีเมลไปแล้ว — เข้าสู่ระบบได้เลย
> · ถ้ายังไม่เคยยืนยัน — สมัครด้วยอีเมลเดิมอีกครั้ง ระบบจะส่งลิงก์ใหม่ให้

**และต้องแก้ข้อความหน้าสมัครคู่กัน** — ตอนนี้เขียนว่า "อีเมลนี้ถูกใช้สมัครแล้ว
กรุณาเข้าสู่ระบบ หรือกดลืมรหัสผ่าน" ซึ่งสำหรับบัญชีที่ยังไม่ยืนยัน **ชี้ไปทางตันทั้งสองทาง
ทั้งที่ลิงก์ใหม่เพิ่งถูกส่งไปแล้วจริง** ต้องเพิ่มว่าถ้ายังไม่เคยยืนยัน ให้ไปดูกล่องจดหมาย

### A6 · บัญชีที่ไม่มีแถวใน `profiles` ได้สิทธิ์ member เงียบๆ — **ทำแล้ว**

**พบนอกตาราง UAT** ระหว่างทดลอง A4 เมื่อ 17 ก.ย. ผู้ทดสอบลบผู้ใช้แล้วสมัครใหม่
ด้วยอีเมลเดิมไม่ได้ · ตรวจกับฐานจริงพบว่า `siwakornt1999@gmail.com` **ยังอยู่ใน
`auth.users`** (`deleted_at` เป็น null) แต่ไม่มีแถวใน `public.profiles` แล้ว
สิ่งที่ถูกลบคือแถวใน `profiles` ไม่ใช่บัญชีใน Authentication → Users —
**FK cascade วิ่งทางเดียว** (ลบ auth user → ลบ profile ไม่ใช่ทางกลับ)

เดิม `getSession()` เขียนว่า `role: p?.role ?? 'member'` ผลคือบัญชีแบบนั้น:

- **ล็อกอินและใช้งานได้ตามปกติ** ทั้งที่ "ถูกลบไปแล้ว"
- **ไม่โผล่ใน `/admin/users`** ซึ่งอ่านจาก `profiles` — 6 แถวใน profiles แต่ 7 บัญชีใน auth
- แอดมินเปลี่ยนสิทธิ์ไม่ได้ ไม่มีแถวให้ update · `activity_log` ย้อนชื่อไม่ได้

**การลบแถวใน `profiles` จึงดูเหมือนถอดสิทธิ์ แต่จริงๆ แค่ทำให้คนนั้นล่องหน**
ซึ่งตรงข้ามกับสิ่งที่คนกดลบคาดหวังทุกประการ

**แก้แล้ว** — `lib/auth/profileGate.ts` (`decideAccess`) แยกสามสภาวะ:
อ่านล้มเหลว · ไม่มีแถว · `role` ที่ enum ไม่รู้จัก **ทั้งสามปิดประตู**
`getSession()` คืน `null` ซึ่งผู้เรียกทั้ง 40 ที่จัดการอยู่แล้ว ไม่ต้องแก้สักไฟล์

- **ลำดับการตรวจสำคัญ** ดู `hadError` ก่อน `!row` เพราะตอนอ่านล้ม row ก็เป็น null
  ตรวจสลับกันจะรายงาน "ไม่มีโปรไฟล์" ตอนฐานข้อมูลพัง แล้วไล่หาสาเหตุผิดที่
- **หน้า `/login` ต้องเช็คซ้ำเพื่อกันการวนลูป** ไม่ใช่เพื่อความปลอดภัย —
  ไม่มีด่านนี้ ผู้ใช้จะล็อกอินสำเร็จแล้วถูกเด้งกลับมาหน้าเดิมไปเรื่อยๆ
- ข้อความบัญชีไม่มีโปรไฟล์**ไม่บอกให้ลองใหม่** เพราะลองอีกกี่ครั้งก็เหมือนเดิม

**งานที่เหลือของข้อนี้ ผู้ใช้ต้องทำเองที่ Supabase:** ลบบัญชี `eda3cf9c-ad96-4fcd-88e2-88e604b06561`
(`siwakornt1999@gmail.com`) ที่ Authentication → Users เพื่อคืนอีเมลให้ใช้สมัครได้อีก
**และต่อไปให้ลบที่นั่นเสมอ ไม่ใช่ลบแถวใน `profiles`**

### A5 · AU-08 — สามหน้า auth ไม่มีทางกลับหน้าแรก — **ทำแล้ว**

`/login` `/signup` `/forgot-password` เป็นทางตันสำหรับคนที่ยังไม่มีบัญชีและอยาก
ดูว่าเว็บนี้คืออะไร ซึ่งขัดกับเหตุผลที่หน้าแรกถูกทำให้เปิดได้โดยไม่ต้องล็อกอิน

---

## กอง B — แก้เอกสาร UAT ไม่แตะโค้ด

**ทุกข้อในกองนี้ระบบทำงานถูกอยู่แล้ว** เคสเขียนไว้ไม่ตรงกับสิ่งที่ระบบเป็น

| เคส | ปัญหา | ทำอะไร |
|---|---|---|
| SA-02 | ช่องเลือกไฟล์กรองเหลือ PDF ทดสอบผ่านหน้าจอไม่ได้ | ย้ายไปฉบับผู้พัฒนา ยิง API ตรงด้วยไฟล์ที่ไม่ใช่ PDF |
| ST-05 · JB-05 | `maxLength` กันไม่ให้พิมพ์เกิน จึงไม่มีข้อความบอกเพดาน | เขียนใหม่ให้ตรงกับพฤติกรรมจริง + เพิ่มเคสฝั่ง API ในฉบับผู้พัฒนา |
| SA-05 | ระบบไม่มี URL รายคนสำหรับผลประเมิน | **ตัดทิ้ง** — ST-11 และ ST-12 พิสูจน์เรื่องเดียวกันไปแล้วและผ่านทั้งคู่ |
| DM-10 · DM-11 | คิวรอตรวจเติมจากการอัปโหลดด้วยมือไม่ได้ | เปลี่ยนเป็น "ทดสอบไม่ได้ในเวอร์ชันนี้" พร้อมเหตุผล |

**สามเคสแรกเป็นรูปแบบเดียวกัน: หน้าจอกันไว้แล้ว จึงทดสอบด่านจริงไม่ได้**
เหมือน AD-02 / ST-11 / ST-12 ที่แยกไปฉบับผู้พัฒนาตั้งแต่ต้น — **ด่านจริงอยู่ที่
เซิร์ฟเวอร์ การทดสอบว่า "กดในหน้าเว็บไม่ได้" ไม่ได้ทดสอบด่านเลย**

### ยืนยันกับโค้ดแล้ว: คิวรอตรวจไม่มีเส้นทางถึงในเวอร์ชันนี้

`classifyRow` ถูกเรียกจาก `scripts/sync-phantombuster.ts` **ที่เดียว**
`app/api/ingest/route.ts` ไม่เรียกเลย การอัปโหลด CSV ด้วยมือจึงเข้า `candidates`
ตรงทุกแถวไม่ว่าข้อมูลจะครบหรือไม่

ฟีเจอร์มีจริงในโค้ดและมีเทสต์ครอบ แต่**เปิดใช้ไม่ได้จนกว่าจะมีบัญชี PhantomBuster**
ข้อนี้ต้องเขียนลงเล่มตรงๆ ว่าเป็นข้อจำกัดที่รู้ตัว ไม่ใช่ปล่อยให้ดูเหมือนทดสอบครบ

### ทำทีหลังสุดหรือไม่ทำก็ได้

- **เปลี่ยนรหัส DM-08…DM-11 เป็น IM-xx** — เหตุผลถูก (ไม่ได้ทดสอบที่ `/candidates`
  แล้ว) แต่ต้องแก้ `results.md` กับเอกสารพร้อมกัน ไม่งั้น `mergeUat.test.ts` แดง
  **ได้ประโยชน์แค่ความสวยงาม เหลือ 8 วัน — ทำเป็นข้อสุดท้ายหรือข้ามไป**
- **เรียง XR-04 / XR-06 ให้ถูก** — เรื่องเดียวกัน
- **DB-08** (รีเฟรชสามครั้งแล้วลำดับแท่งไม่สลับ) ผู้ทดสอบบอกว่าไม่เข้าใจว่าเช็คอะไร
  → เขียนคำอธิบายเพิ่มในเคส ไม่ใช่ตัดทิ้ง มันดักเรื่องที่เกิดได้จริง

---

## กอง C — ลงเล่มอย่างเดียว ไม่แก้โค้ด

### LLM แกว่งถึงขั้นเปลี่ยนโครงตัวกรอง ไม่ใช่แค่ตัวเลข

พบ 18 ก.ย. ระหว่างทดสอบ A1 · คำค้นเดียวกันรันสามครั้งได้ชิปสกิลคนละแบบ:

| รอบ | คำค้น | ชิปสกิลที่ AI แยกให้ |
|---|---|---|
| 1 | Data Scientist | `Data Science` |
| 2 | Data Scientist | **ไม่มีเลย** |
| 3 | UX Designer | `UX` |
| 4 | UX Designer | `UX Design` |

CLAUDE.md บันทึกไว้แล้วว่าคะแนน `analyze` แกว่งได้ 10 แต้มจาก input เดียวกัน
**สิ่งที่เพิ่งเห็นหนักกว่านั้น — มันแกว่งถึงขั้นเปลี่ยนว่าจะมีตัวกรองหรือไม่มี**

**ก่อนแก้ A1 ความแกว่งนี้ทำให้ผลลัพธ์ต่างกันสิ้นเชิง** (มีชิป = 0 คน · ไม่มีชิป = 20 คน)
**หลังแก้แล้วเหลือแค่ความต่างของข้อความอธิบาย** ผลลัพธ์เท่ากันทั้งสองทาง
— การแก้ A1 จึงลดผลของความไม่นิ่งนี้ลงจาก "ใช้งานไม่ได้" เหลือ "ข้อความไม่สม่ำเสมอ"

**รูปแบบความผิดพลาดที่เห็นชัด: AI เอาชื่อตำแหน่งมาพูดซ้ำเป็นสกิล**
"Data Scientist" → "Data Science" · "UX Designer" → "UX Design"
ซึ่งไม่ใช่ทักษะ แต่คือชื่อตำแหน่งเดิมที่อยู่ใน `semanticQuery` อยู่แล้ว เท่ากับ
นับซ้ำสองครั้ง **แก้ได้ด้วยการปรับ prompt แต่ผลของ prompt พิสูจน์ด้วย unit test
ไม่ได้** ต้องวัดซ้ำอย่างน้อย 3 รอบแบบที่ `compare-gemini-models.ts` ทำ
→ **ไม่ทำก่อนส่ง** เขียนเป็นงานถัดไปในเล่ม

**ข้อนี้เสริมข้อห้ามใน `/terms` ข้อ 4** เรื่องห้ามใช้คะแนน AI ตัดสินคนโดยลำพัง —
ไม่ใช่แค่คะแนนที่ไม่นิ่ง แต่**เงื่อนไขที่ใช้คัดคนก็ไม่นิ่งด้วย**

### การค้นด้วยเวกเตอร์ไม่มีแนวคิดเรื่อง "ใกล้ไม่พอ"

คืนเพื่อนบ้านที่ใกล้ที่สุดเสมอ ต่อให้ไม่มีใครเกี่ยวข้องเลย — วัดแล้วว่าตั้งเกณฑ์
ตัดไม่ได้ (ดู A1) ผู้ใช้จึงต้องอ่านคะแนนประกอบเสมอ ไม่ใช่เชื่อว่าทุกคนในผลลัพธ์
คือคนที่ตรงเงื่อนไข

- **คิวรอตรวจยังไม่มีเส้นทางถึง** (ดูข้างบน)
- **แอดมินเปลี่ยนสิทธิ์แอดมินคนอื่นได้** (AD-01) — เป็นข้อจำกัดที่บันทึกไว้แล้วใน
  `lib/auth/roleChange.ts` ว่ากฎนี้กันแค่การลดสิทธิ์ตัวเอง ไม่รับประกันว่าจะเหลือ
  แอดมินเสมอ **เขียนลงเล่มตามที่เป็น อย่าอ้างว่าปิดสนิท**
- **อีเมลส่งจากโดเมน `mail.app.supabase.io`** — ใช้ทดสอบได้ แต่ตอนส่งมอบจริง
  ดูเหมือนไม่ใช่ระบบขององค์กร · ต่อ SMTP เองเมื่อพร้อม
- **Gemini ถอดชื่อเฉพาะได้ไม่ดีนัก** (ST-09) — สอดคล้องกับที่ CLAUDE.md บันทึกไว้
  เรื่องความไม่นิ่งของผลลัพธ์ ใช้เสริมบทข้อจำกัดได้
- **ข้อเสนอแนะฟีเจอร์** — ลบ shortlist ทั้งรายการ (SL-04) · ตัวกรองระดับการศึกษา
  ใน X-ray (XR-01) · เลขเวอร์ชันตกไปอีกหน้าตอนพิมพ์ (PB-08) · เลื่อนดูบันทึก
  กิจกรรมย้อนหลัง (AD-04)
- **เอาข้อมูลสังเคราะห์ออกก่อน deploy จริง** (JB-09)

---

## ลำดับการทำ

1. **A2 · A3 · A5** — ไม่ต้องตัดสินใจอะไรเพิ่ม ทำได้เลย
2. **A4** — ทดลองเรื่องอีเมลฉบับที่สองก่อน แล้วค่อยเขียนข้อความ
3. **A1** — ต้องวัดเกณฑ์ก่อน
4. **กอง B** ทั้งหมด — ไม่ต้องรอเทสต์ ทำคู่ขนานได้
5. **กอง C** — ทำตอนแก้เล่ม 21-23

ทุกข้อในกอง A: เขียนเทสต์ก่อน → ทำให้ผ่าน → commit → break check → ย้อนกลับ
